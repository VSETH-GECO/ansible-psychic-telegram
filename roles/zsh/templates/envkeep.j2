# Managed by ansible (role zsh), do not edit!
# This keeps DEFAULT_USER, so that the shell knows which user originally logged in.
Defaults env_keep += "DEFAULT_USER"
# This keeps TMUX, so that the shell knows that we still run in TMUX
Defaults env_keep += "TMUX"
